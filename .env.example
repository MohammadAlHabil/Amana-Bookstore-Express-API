# Environment Configuration Example
NODE_ENV=development

# Server Configuration
PORT=3000

# CORS Configuration (Use * for all origins in development, specific domain in production)
CORS_ORIGIN=*

# Logging Configuration
LOG_LEVEL=dev
